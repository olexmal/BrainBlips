{
  "name": "agentic-ai",
  "version": "0.0.0",
  "scripts": {
    "docs:serve": "cd website && npm run start",
    "docs:build": "cd website && npm run build",
    "clickup:sync": "node scripts/sync-clickup.mjs"
  },
  "devDependencies": {}
}
